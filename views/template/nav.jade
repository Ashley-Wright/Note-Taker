.contain-to-grid
  nav.top-bar
    ul.title-area
      li.name
        h1: a(href="/") Note Taker
      li.toggle-topbar.menu-icon
        a(href="#")
          span Menu

    section.top-bar-section
      ul.left
      ul.right
        li.divider
        li.has-form: a#sign-in-button.button.radius(href="#", data-email = 'guest') Sign In


        //- if(user)
        //-   li.has-form: a#authentication-button.button.radius.alert(href="#", data-email = 'user.email')= user.email
        //- else
        //-   li.has-form: a#authentication-button.button.radius(href="#", data-email = 'guest') Login | Sign Up